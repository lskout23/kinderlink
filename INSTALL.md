# KinderLink — νέα, ανεξάρτητη εγκατάσταση

Το KinderLink είναι **διανεμόμενο πακέτο ανεξάρτητης εγκατάστασης σε νέα κενή βάση**.
Οι παρακάτω οδηγίες καλύπτουν την εγκατάσταση και την απομονωμένη αξιολόγηση· δεν
είναι διαδικασία αναβάθμισης ή μεταφοράς δεδομένων. **Δεν έχει γίνει deployment
σε παραγωγή από τον agent.** Η επιλογή hosting, οι ιδιωτικές ρυθμίσεις και η
παραγωγική εγκατάσταση αποτελούν ξεχωριστή εργασία του υπεύθυνου λειτουργίας.
Μην δείξετε κανένα εργαλείο εγκατάστασης σε βάση σχολείου που χρησιμοποιείται ήδη.

## 1. Προϋποθέσεις

- PHP **8.3+**, σε υποστηριζόμενη έκδοση, τόσο στο CLI όσο
  και στο Apache. Απαιτούνται `pdo_mysql`, `mbstring`, `fileinfo`, `openssl`,
  `curl`. Συνιστάται `gd` για λειτουργίες εικόνων. Ελέγξτε και τα δύο PHP runtimes.
- MySQL 8.0+ ή MariaDB 10.6+, με InnoDB, `utf8mb4` και διαθέσιμα
  `GET_LOCK`/`RELEASE_LOCK`. Η συμβατότητα πρέπει να επιβεβαιωθεί στον επιλεγμένο server.
- Apache 2.4 με `mod_rewrite`, ενεργό τοπικό virtual host και εφαρμογή των κανόνων
  [.htaccess](.htaccess) μέσω κατάλληλου `AllowOverride`. Συνιστάται `mod_headers`.
  Η ρίζα του virtual host πρέπει να είναι ο φάκελος της **νέας** εφαρμογής.
- **Δεν απαιτείται Composer.** Οι υλοποιήσεις email χρησιμοποιούν native PHP
  SMTP και το HTTPS API του Brevo, χωρίς εξάρτηση PHPMailer.
  Μην αντιγράφετε vendor ή μυστικά από άλλη εγκατάσταση.
- Για τον απομονωμένο έλεγχο απαιτούνται CLI `proc_open` και τοπικός χρήστης DB
  με δικαίωμα δημιουργίας/διαγραφής **δοκιμαστικών** βάσεων. Αυτά τα δικαιώματα
  δεν προορίζονται για τον χρήστη βάσης της κανονικής εφαρμογής.

## 2. Απομονωμένη βάση — schema μία φορά μόνο

1. Με τον υπεύθυνο βάσης δημιουργήστε μία **καινούργια** βάση, π.χ. `kinderlink_new`,
   με `utf8mb4_unicode_ci`. Βεβαιωθείτε ότι δεν περιέχει κανέναν πίνακα/εγγραφή.
2. Δημιουργήστε αποκλειστικό χρήστη με ισχυρό ιδιωτικό κωδικό και πρόσβαση μόνο
   στη νέα βάση. Μη χρησιμοποιείτε λογαριασμό ή όνομα βάσης άλλου σχολείου.
3. Επιλέξτε ρητά τη νέα βάση στον SQL client και εισαγάγετε ολόκληρο το
   [database/schema.sql](database/schema.sql) **μία φορά**. Η εφαρμογή δεν
   δημιουργεί βάση για λογαριασμό σας και το create-admin δεν εισάγει SQL.
4. Μην εισαγάγετε επιπλέον migrations, SQL dumps ή real-school seeds από το
   γονικό έργο. Το schema περιλαμβάνει ήδη απουσίες, φωτογραφίες, login attempts,
   συνομιλίες γονέων και `temp_password_expires`.
5. Επιβεβαιώστε 18 πίνακες, μηδέν χρήστες/παιδιά/τμήματα και μόνο τα γενικά
   defaults. Οι απουσίες πρέπει να εμφανίζονται έτοιμες χωρίς migration.

Το schema δεν είναι επανεκτελέσιμο: δεν χρησιμοποιεί `IF NOT EXISTS` και
διατηρεί ενεργούς τους ελέγχους ξένων κλειδιών. Μην συνεχίσετε με επιλογή
«ignore errors»/`--force`. Η DDL στη MySQL δεν είναι συνολικά transactional:
αν αποτύχει εισαγωγή, ελέγξτε το σφάλμα και ξεκινήστε με **άλλη νέα κενή βάση**,
όχι με επανεισαγωγή πάνω στη μερική εγκατάσταση.

## 3. Ρυθμίσεις εφαρμογής

Χρησιμοποιήστε το [.env.example](.env.example) ως υπόδειγμα για δικό σας τοπικό
αρχείο `.env.local` μέσα στον νέο φάκελο, ή ορίστε τις μεταβλητές στο περιβάλλον
του συγκεκριμένου virtual host. **Μην αντιγράψετε κανένα αρχείο env του γονικού έργου.**

- Συμπληρώστε `DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS`, `DB_CHARSET=utf8mb4`
  για τη νέα βάση. Επιβεβαιώστε ότι το runtime χρησιμοποιεί αυτά τα στοιχεία.
- Ρυθμίστε `APP_URL` και, εάν χρειάζεται, `APP_BASE_URL` για το νέο τοπικό URL.
  Επιλέξτε ξεχωριστό `SESSION_NAME` ώστε να μη μοιράζεστε cookies με άλλη εφαρμογή.
- Για τοπική αξιολόγηση ορίστε κατάλληλο `APP_ENV`. Μην εμφανίζετε debug/stack
  traces σε μη έμπιστους χρήστες. Κρατήστε τα μυστικά έξω από Git, screenshots και logs.
- Η ρύθμιση `email_mode` βρίσκεται στη βάση, όχι σε διακόπτη του installer.
  Η αρχική τιμή είναι **virtual**. Αφήστε SMTP/Brevo μυστικά μη ρυθμισμένα έως
  τον ελεγχόμενο έλεγχο αποστολής.
- Ελέγξτε μήπως κληρονομημένες μεταβλητές συστήματος υπερισχύουν των τοπικών
  ρυθμίσεων. Το CLI provisioning παρακάτω αποφεύγει αυτό το πρόβλημα με δικό του prefix.

## 4. Πρώτος διαχειριστής — μόνο ιδιωτικό CLI

Το [tools/create-admin.php](tools/create-admin.php) **δεν φορτώνει config ή αρχεία
env**, δεν διαβάζει γονικό φάκελο, δεν στέλνει email και δεν εισάγει SQL. Απαιτεί
ήδη εισαγμένο schema, κανέναν διαχειριστή (ούτε ανενεργό) και κανένα λειτουργικό
δεδομένο σχολείου. Δεν είναι εργαλείο ανάκτησης υπάρχοντος λογαριασμού.

Στο ιδιωτικό περιβάλλον της συγκεκριμένης CLI διεργασίας ορίστε ρητά:

| Μεταβλητή | Τιμή |
| --- | --- |
| `KINDERLINK_INSTALL_DB_HOST` | Host της αποκλειστικά νέας βάσης |
| `KINDERLINK_INSTALL_DB_PORT` | Προαιρετικά, προεπιλογή `3306` |
| `KINDERLINK_INSTALL_DB_NAME` | Το ακριβές όνομα της νέας βάσης |
| `KINDERLINK_INSTALL_DB_USER` | Ο αποκλειστικός χρήστης βάσης |
| `KINDERLINK_INSTALL_DB_PASS` | Ο δικός σας ιδιωτικός κωδικός DB, υποχρεωτικά ορισμένος |

Τα απλά `DB_*` της εφαρμογής **δεν χρησιμοποιούνται** από αυτό το εργαλείο.
Περάστε τα μυστικά μέσω ιδιωτικού prompt/διαχειριστή μυστικών στο περιβάλλον
διεργασίας, όχι ως κυριολεκτική τιμή σε εντολή που αποθηκεύεται στο history.
Σε Windows/PowerShell 5.1 η κενή τιμή environment συχνά αφαιρεί τη μεταβλητή·
χρησιμοποιήστε χρήστη βάσης με πραγματικό ισχυρό κωδικό.

Μόνο για απομονωμένο τοπικό development με `127.0.0.1` ή `::1`, υποστηρίζεται
το ρητό `KINDERLINK_INSTALL_ALLOW_EMPTY_LOCAL_PASSWORD=1`. Το χρησιμοποιεί το
test harness για τη συμπεριφορά Windows με κενές μεταβλητές· δεν ισχύει για
απομακρυσμένους hosts και δεν προτείνεται για σχολική εγκατάσταση.

Από τον νέο φάκελο, με δικό σας username/email (τα παρακάτω είναι μόνο παράδειγμα):

```powershell
php tools/create-admin.php --confirm-new-install --username=my.school.admin --email=owner@example.invalid
```

Το username ακολουθεί την πολιτική της εφαρμογής: 4–50 λατινικοί χαρακτήρες,
αριθμοί και `@ . _ -`. Αντικαταστήστε το email του παραδείγματος με ιδιωτική,
έγκυρη διεύθυνση που ελέγχετε. **Δεν υπάρχει παράμετρος password** ούτε προεπιλεγμένος κωδικός.

- Παράγεται τοπικά κρυπτογραφικά τυχαίος κωδικός 32 χαρακτήρων με `random_bytes`.
  Στη βάση γράφεται μόνο bcrypt hash, όπως χρησιμοποιεί το `Auth`.
- Μόνο μετά το επιτυχημένο commit εμφανίζεται μία γραμμή JSON με `status`,
  `username`, `email`, `initial_password`. **Όλη η έξοδος είναι ιδιωτική**:
  όχι CI logs, κοινόχρηστη κονσόλα, screen recording, chat ή αρχείο ανακατεύθυνσης.
  Αποθηκεύστε τον κωδικό σε password manager και καθαρίστε το scrollback.
- Συνδεθείτε αμέσως και αλλάξτε κωδικό από **Προσωπικά Στοιχεία**. Η αλλαγή
  είναι οδηγία ασφαλείας, όχι υποχρεωτική ροή που επιβάλλεται από τον installer.
- Το `temp_password_expires` του πρώτου admin είναι `NULL`. Δεν αποστέλλεται
  προσωρινός κωδικός με email ούτε υπόσχεται ο installer αυτόματη λήξη του.
- Επαναληπτική εκτέλεση αρνείται να γράψει ή να εμφανίσει κωδικό. Αν χαθεί η
  αρχική έξοδος, μην περιμένετε επανεμφάνιση και μην διαγράψετε χρήστες σε λειτουργική βάση.
- Οι ταυτόχρονες εκτελέσεις του εργαλείου σειριοποιούνται με MySQL advisory lock
  ανά βάση, timeout 15 δευτερολέπτων, transaction και αποδέσμευση στο `finally`.
  Κρατήστε την εφαρμογή εκτός πρόσβασης μέχρι να τελειώσει το provisioning:
  το advisory lock δε δεσμεύει άλλους writers που δεν χρησιμοποιούν το εργαλείο.
- Αφαιρέστε τις μεταβλητές provisioning από το περιβάλλον όταν τελειώσετε.

## 5. Προφίλ και επωνυμία σχολείου

Στα **Προσωπικά Στοιχεία** συμπληρώστε όνομα, username και email του πραγματικού
διαχειριστή. Στις **Παραμέτρους**, αλλάξτε το `school_name` από «Το σχολείο μου»
στη δική σας επωνυμία. Το προσωπικό όνομα διαχειριστή και η επωνυμία σχολείου
είναι διαφορετικές ρυθμίσεις. Προσθέστε μόνο δικά σας τμήματα/εκπαιδευτικούς/
παιδιά όταν ολοκληρωθούν οι έλεγχοι πρόσβασης και προστασίας δεδομένων.

## 6. Email: virtual, SMTP και Brevo

1. Ξεκινήστε με `email_mode=virtual` και συνθετικά δεδομένα. Ελέγξτε πρότυπο,
   υπόμνημα, ύπνο, παραλήπτες και αποκλεισμό παιδιών με απουσία.
2. Για SMTP ορίστε `EMAIL_PROVIDER=smtp`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURE`
   (`tls`/587 ή `ssl`/465 σύμφωνα με τον πάροχο), `SMTP_USER`, `SMTP_PASS`,
   `SMTP_FROM`, `SMTP_FROM_NAME`. Επαληθεύστε αποστολέα/domain, TLS/CA και
   SPF/DKIM/DMARC. Μην απενεργοποιείτε την επαλήθευση πιστοποιητικών.
3. Για Brevo ορίστε `EMAIL_PROVIDER=brevo`, ιδιωτικό `BREVO_API_KEY` και
   επαληθευμένο αποστολέα. Χρησιμοποιήστε το επίσημο HTTPS API endpoint·
   μην κατευθύνετε κλειδιά σε μη έμπιστο endpoint. Ελέγξτε όρια και σφάλματα API.
4. Μόνο σε απομονωμένη εγκατάσταση, με εξουσιοδοτημένους δοκιμαστικούς
   παραλήπτες και χωρίς πραγματικά παιδιά, επιτρέψτε προσωρινά πραγματική
   αποστολή για να ελεγχθεί ο πάροχος. Η επιτυχία σε virtual **δεν** πιστοποιεί
   SMTP/Brevo. Επιστρέψτε σε virtual μετά τον έλεγχο.
5. Ελέγξτε χωριστά καθημερινές ενημερώσεις, ανακοινώσεις και ανάκτηση λογαριασμού.
   **Προσοχή:** στον τρέχοντα AuthController η ανάκτηση χρησιμοποιεί απευθείας
   SMTP, όχι τον dispatcher Brevo/virtual. Το virtual δεν εγγυάται ότι κάθε ροή
   δεν στέλνει email. Χωρίς εγκεκριμένο test SMTP/αποκλεισμό εξερχόμενου δικτύου,
   μην ενεργοποιείτε τις φόρμες ανάκτησης κατά τις δοκιμές.

## 7. Απομονωμένοι έλεγχοι εγκατάστασης και λήξης κωδικού

Το [tools/test-fresh-install.php](tools/test-fresh-install.php) δεν διαβάζει
κανένα αρχείο env ούτε τις γενικές μεταβλητές DB της εφαρμογής. Ορίστε μόνο:

| Μεταβλητή | Χρήση |
| --- | --- |
| `KINDERLINK_TEST_DB_USER` | Υποχρεωτικός τοπικός χρήστης δοκιμών |
| `KINDERLINK_TEST_DB_PASS` | Υποχρεωτικά ορισμένος ιδιωτικός κωδικός δοκιμών |
| `KINDERLINK_TEST_DB_HOST` | Προαιρετικά `127.0.0.1` (default), `localhost` ή `::1` |
| `KINDERLINK_TEST_DB_PORT` | Προαιρετικά `3306` |

Ο λογαριασμός χρειάζεται CREATE/DROP για τοπικές δοκιμαστικές βάσεις και
δικαιώματα schema/DML μέσα σε αυτές. Χρησιμοποιήστε ξεχωριστό τοπικό MySQL
instance όπου είναι εφικτό, **όχι tunnel/proxy προς production**.

```powershell
php tools/test-fresh-install.php
```

Δεν δέχεται όνομα βάσης, διαδρομή SQL ή άλλα arguments. Δημιουργεί πάντα βάση
`kinderlink_fresh_test_` με 24 τυχαία hex ψηφία, χωρίς `IF NOT EXISTS`. Σε
σύγκρουση ονόματος αποτυγχάνει χωρίς να αγγίξει υπάρχουσα βάση. Εισάγει μόνο
το τοπικό schema και εκτελεί subprocesses του create-admin με ελάχιστο,
ρητά ορισμένο περιβάλλον και τη δική του βάση.

Ελέγχει πίνακες/στήλες, τύπους reset, κενές λειτουργικές εγγραφές, attendance
readiness, virtual/defaults, υπόμνημα/ύπνο και bcrypt/role. Επιπλέον ελέγχει
άρνηση δεύτερου admin, άρνηση ανενεργού admin και δύο ταυτόχρονες εκτελέσεις.
**Δεν εκτυπώνει password, stdout ή stderr των subprocesses**, ούτε όταν αποτύχει.
Δεν φορτώνει mail transport και δεν στέλνει email.

Η διαγραφή γίνεται σε `finally`, με δεύτερη προσπάθεια σύνδεσης και shutdown
fallback. Απώλεια server, δικαιωμάτων DROP ή βίαιος τερματισμός της διεργασίας
μπορεί να εμποδίσουν οποιοδήποτε cleanup: τότε αφαιρέστε μόνο την αναφερόμενη
τυχαία δοκιμαστική βάση, ποτέ βάση σχολείου.

Το [tools/test-auth-expiry.php](tools/test-auth-expiry.php) δέχεται τις ίδιες
`KINDERLINK_TEST_DB_USER/PASS` και προαιρετικές `HOST/PORT` παραπάνω. Εκτελείται
από CLI χωρίς δημόσια arguments. Δημιουργεί ξεχωριστή τυχαία βάση
`kinderlink_auth_test_` με 24 hex ψηφία μόνο σε loopback, εισάγει το τοπικό schema
και τη διαγράφει σε `finally` με shutdown fallback. Δεν φορτώνει config/env
της εφαρμογής ή του γονικού έργου και δεν κληρονομεί ρυθμίσεις SMTP/DB στα workers.

Κάθε περίπτωση καλεί το πραγματικό `AuthController::login()` σε νέο subprocess,
με injected PDO, έγκυρο CSRF token και πραγματικό PHP session σε απομονωμένο
in-memory handler. Ελέγχει ότι ληγμένος προσωρινός κωδικός απορρίπτεται χωρίς
session authentication, μελλοντική λήξη επιτρέπει login και καθαρίζεται σε `NULL`,
αρχικό `NULL` επιτρέπει login και λάθος κωδικός απορρίπτεται. Επιβεβαιώνει επίσης
session regeneration στην επιτυχία και ότι ο αποθηκευμένος κωδικός δεν αλλάζει.
Δεν καλεί ανάκτηση κωδικού, δεν στέλνει SMTP/email και δεν εμφανίζει μυστικά ή
ιδιωτικά subprocess streams. Οι ίδιοι περιορισμοί cleanup ισχύουν και εδώ.

Ο παλαιότερος [tools/test-attendance.php](tools/test-attendance.php) διατηρεί τη
συμπεριφορά του και αλλάζει μόνο το prefix σε `kinderlink_attendance_test_`.
Φορτώνει το config της νέας εφαρμογής και χρησιμοποιεί `127.0.0.1:3306`· δεν έχει
το νέο απομονωμένο πρωτόκολλο περιβάλλοντος. Μην τον τρέξετε με credentials
πραγματικού σχολείου. Το [tools/test-email-sleep.php](tools/test-email-sleep.php)
είναι χωριστός έλεγχος rendering χωρίς DB/email.

Η τελική επαλήθευση περιλαμβάνει lint και τους παραπάνω ελέγχους σε απομονωμένο
περιβάλλον, με καταγραφή των αποτελεσμάτων από τον υπεύθυνο ενσωμάτωσης.

## 8. Ασφάλεια, αντίγραφα και εκκρεμότητες

### Έλεγχοι πακέτου — 24/09/2026

PHP 8.3.30 / MySQL 8.4.3: lint 51 αρχείων, 74 assertions πλήρους schema και
πρώτου admin, 8 login expiry, 34 απουσιών, 18 rendering ύπνου πέρασαν.
Πέντε οθόνες ελέγχθηκαν σε 390px/1440px χωρίς overflow ή broken images.
Ελέγχθηκαν manifest και απομόνωση PWA caches. Το Apache επέστρεψε 403 για
env/schema/tools/config/models/docs και 200 για το δημόσιο badge. Gitleaks:
δεν εντοπίστηκαν μυστικά. Πρόκειται για τοπικούς ελέγχους, όχι για πιστοποίηση
production, SMTP, πραγματικών φωτογραφιών ή συμμόρφωσης προσωπικών δεδομένων.

- Μετά το schema αφαιρέστε περιττά CREATE/ALTER/DROP δικαιώματα από τον χρήστη
  εφαρμογής. Μη λειτουργείτε με root. Μην αφήνετε τον web server να τροποποιεί
  κώδικα/config· δώστε εγγραφή μόνο στους απαιτούμενους φακέλους αποθήκευσης.
- Προστατέψτε env, storage, SQL, logs και tools από άμεση HTTP πρόσβαση και
  επαληθεύστε στην πράξη τους κανόνες Apache. Τα νέα CLI εργαλεία επιστρέφουν
  404 εκτός CLI, αλλά αυτό δεν αντικαθιστά την προστασία ολόκληρου του server.
- Προστατέψτε τα μυστικά με ACLs/permissions, εκτός Git και δημόσιου web access.
  Ποτέ `777` ή καθολική εγγραφή. Χρησιμοποιήστε HTTPS για μη τοπική πρόσβαση.
- Σχεδιάστε κρυπτογραφημένα, περιορισμένης πρόσβασης backups βάσης και uploads,
  με πολιτική διατήρησης και δοκιμή επαναφοράς σε ξεχωριστή βάση. Ελέγξτε
  πολιτικές φωτογραφιών, γονική πρόσβαση, συγκατάθεση και ελαχιστοποίηση δεδομένων.
- Το login διαβάζει το `temp_password_expires` από το πλήρες schema και
  εφαρμόζει τον υπάρχοντα έλεγχο λήξης. Η επιτυχής σύνδεση καθαρίζει το πεδίο·
  δεν αλλάζει η γενική συμπεριφορά ανάκτησης ή αλλαγής κωδικού.
- Ολοκληρώστε ξεχωριστό έλεγχο ασφάλειας και εξαρτήσεων πριν εξεταστεί production.
  Οι οδηγίες αυτές **δεν εξουσιοδοτούν και δεν υλοποιούν deployment**.
- Η ονομασία **KinderLink δεν έχει ελεγχθεί νομικά ως εμπορικό σήμα**. Απαιτείται
  ανεξάρτητος έλεγχος δικαιωμάτων πριν από δημόσια/εμπορική χρήση.